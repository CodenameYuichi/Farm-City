<p id="notice"><%= notice %></p>

<p>
  <strong>Picture:</strong>
  <%= image_tag @image.picture_url.to_s %>
</p>

<p>
  <strong>Place:</strong>
  <%= @image.place.name %>
</p>

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<%= link_to 'Edit', edit_place_image_path(@image.place.id, @image.id) %> |
<%= link_to 'Back', place_images_path %>
