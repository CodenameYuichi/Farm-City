<div class="text-center">
  <h1>Galeria <%= @images.first.place.name %></h1>
</div>
<div>
  <%= link_to 'Powrót do miejsca', place_path(@images.first.place) %>
</div>
<% @images.each_slice(3) do |slice| %>
  <div class="row">
  <% slice.each do |image| %>
    <div class="col-sm-4">
      <div class="well">
        <%= link_to place_image_path(image.place.id, image.id) do %>
          <%= image_tag image.picture.large.to_s, class: 'gallery img-responsive' %>
        <% end %>
        <div class='text-center'><%= image.name %></div>
        <div class='text-right'>
          <%= link_to 'Destroy', place_image_path(image.place.id, image.id), method: :delete, data: { confirm: 'Are you sure?' } %>
        </div>
      </div>
    </div>
  <% end %>
  </div>
<% end %>
