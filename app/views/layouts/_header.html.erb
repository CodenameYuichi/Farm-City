<nav id='menu' class='navbar navbar-inverse navbar-default navbar-fixed-top' role='navigation'>
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#my-menu">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to user_signed_in? ? dashbords_path : '/', class: 'navbar-brand' do %>
        Farm City
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="my-menu">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to(t('.dashboard'), dashbords_path) %></li>
          <li><%= link_to(t('.places'), places_path) %></li>
        </ul>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to(t('.edit_profile'), edit_user_registration_path(current_user.id)) %></li>
          <li><%= link_to(t('.edit_data'), edit_user_path(current_user.id)) %></li>
          <li><%= link_to(t('.logout'), destroy_user_session_path, method: :delete) %></li>
        <% else %>
          <li><%= link_to(t('.sign_in'), new_user_session_path) %></li>
          <li><%= link_to(t('.sign_up'), new_user_registration_path) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

